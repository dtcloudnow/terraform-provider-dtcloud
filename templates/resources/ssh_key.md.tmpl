{{- /*
Page template for dtcloud_ssh_key: the one place for this page's hand-written
text. The summary sentence, the schema and the examples are generated; see the
"Documentation" section of README.md. Section order: summary, Example Usage,
notes, Schema, Behaviour worth knowing, Timeouts, Import.
*/ -}}
---
# generated by tfplugindocs -- edit templates/resources/ssh_key.md.tmpl, the
# Description strings in dtcloud/sshkey/resource_ssh_key.go, or the example under
# examples/resources/dtcloud_ssh_key/, then run `make docs`.
page_title: "{{ .Name }} {{ .Type }} - {{ .ProviderName }}"
subcategory: "Access"
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{ .Name }} ({{ .Type }})

{{ .Description | trimspace }}

The API addresses keys by name and has no update endpoint, so the resource ID is the key name, and
changing either `name` or `public_key` replaces the key.

## Example Usage

{{ tffile .ExampleFile }}

{{ .SchemaMarkdown | trimspace }}

## Behaviour worth knowing

* **Trailing whitespace in `public_key` is ignored**, so the newline `file()` usually adds does not
  show as a change.
* **A key the platform reports as deleted counts as gone** and drops out of state.

## Timeouts

Only `create` has a timeout, defaulting to 2 minutes. Creating a key is synchronous, so this bounds a
hung request rather than a wait. Override it with a `timeouts` block:

```terraform
resource "dtcloud_ssh_key" "deploy" {
  # ...

  timeouts {
    create = "5m"
  }
}
```

## Import

{{ codefile "shell" .ImportFile }}
