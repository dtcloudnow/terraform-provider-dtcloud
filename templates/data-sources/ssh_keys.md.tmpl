{{- /*
Page template for the dtcloud_ssh_keys data source: the one place for this
page's hand-written text. The summary sentence, the schema and the example are
generated; see the "Documentation" section of README.md. Section order: summary,
Example Usage, notes, Schema, Behaviour worth knowing.
*/ -}}
---
# generated by tfplugindocs -- edit templates/data-sources/ssh_keys.md.tmpl, the
# Description strings in dtcloud/sshkey/datasource_ssh_keys.go, or the example under
# examples/data-sources/dtcloud_ssh_keys/, then run `make docs`.
page_title: "{{ .Name }} {{ .Type }} - {{ .ProviderName }}"
subcategory: "Access"
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{ .Name }} ({{ .Type }})

{{ .Description | trimspace }}

The list endpoint reports a name and a creation time, and nothing else. Fingerprints and public keys
would mean one more call per key, so look a key up with `dtcloud_ssh_key` when you need those.

## Example Usage

{{ tffile .ExampleFile }}

{{ .SchemaMarkdown | trimspace }}
