{{- /*
Page template for dtcloud_router_interfaces: the one place for this page's hand-written text.
The summary sentence, the schema and the examples are generated; see the
"Documentation" section of README.md.
*/ -}}
---
# generated by tfplugindocs -- edit templates/data-sources/router_interfaces.md.tmpl, the Description
# strings in dtcloud/router/datasource_router_interfaces.go, or the example under
# examples/data-sources/dtcloud_router_interfaces/, then run `make docs`.
page_title: "{{ .Name }} {{ .Type }} - {{ .ProviderName }}"
subcategory: "Networking"
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{ .Name }} ({{ .Type }})

{{ .Description | trimspace }}

The list is as the platform returns it, external gateway first, and includes interfaces Terraform
does not manage.

## Example Usage

{{ tffile .ExampleFile }}

{{ .SchemaMarkdown | trimspace }}

## Behaviour worth knowing

!> **`id` means two different things.** The endpoint puts a **port id** there for an internal
interface and a **subnet id** for the external gateway, and there is no way to make it mean one.
Check `type` first: only an internal interface's id is something `dtcloud_router_interface` can be
imported with or detached by.

* **`network_id` and `subnet_id` are not reported for the external gateway.**
