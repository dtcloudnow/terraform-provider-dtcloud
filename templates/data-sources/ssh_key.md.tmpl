{{- /*
Page template for the dtcloud_ssh_key data source: the one place for this page's
hand-written text. The summary sentence, the schema and the example are
generated; see the "Documentation" section of README.md. Section order: summary,
Example Usage, notes, Schema, Behaviour worth knowing.
*/ -}}
---
# generated by tfplugindocs -- edit templates/data-sources/ssh_key.md.tmpl, the
# Description strings in dtcloud/sshkey/datasource_ssh_key.go, or the example under
# examples/data-sources/dtcloud_ssh_key/, then run `make docs`.
page_title: "{{ .Name }} {{ .Type }} - {{ .ProviderName }}"
subcategory: "Access"
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{ .Name }} ({{ .Type }})

{{ .Description | trimspace }}

Use it to reference a key that Terraform did not create. Declaring one as a resource instead would
hand Terraform ownership of it, and a later destroy would remove it.

## Example Usage

{{ tffile .ExampleFile }}

{{ .SchemaMarkdown | trimspace }}

## Behaviour worth knowing

* **A key that does not exist is an error**, and so is a key the platform reports as deleted.
