{{- /*
Page template for the dtcloud_storage_policies data source: the one place for
this page's hand-written text. The summary sentence, the schema and the example
are generated; see the "Documentation" section of README.md. Section order:
summary, Example Usage, notes, Schema, Behaviour worth knowing.
*/ -}}
---
# generated by tfplugindocs -- edit templates/data-sources/storage_policies.md.tmpl,
# the Description strings in dtcloud/volume/datasource_storage_policies.go, or the
# example under examples/data-sources/dtcloud_storage_policies/, then run
# `make docs`.
page_title: "{{ .Name }} {{ .Type }} - {{ .ProviderName }}"
subcategory: "Storage"
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{ .Name }} ({{ .Type }})

{{ .Description | trimspace }}

`storage_policy` is required on every volume and has no default, so this is how to find the valid
values. The endpoint returns only the policies with a non-zero quota for the project, so what comes
back is what this project can actually create volumes on, not the whole platform catalogue.

## Example Usage

{{ tffile .ExampleFile }}

{{ .SchemaMarkdown | trimspace }}

## Behaviour worth knowing

~> **Pass the `name`, not the `id`.** The volume details endpoint reports the policy by name, so a
volume created with an id reads back as a name and every later plan proposes a change that never
converges.

* **`name` is applied by the provider** and must match exactly.
