{{- /*
Page template for the dtcloud_security_groups data source: the one place for this
page's hand-written text. The summary sentence, the schema and the example are
generated; see the "Documentation" section of README.md. Section order: summary,
Example Usage, notes, Schema, Behaviour worth knowing.
*/ -}}
---
# generated by tfplugindocs -- edit templates/data-sources/security_groups.md.tmpl,
# the Description strings in dtcloud/securitygroup/datasource_security_groups.go, or
# the example under examples/data-sources/dtcloud_security_groups/, then run
# `make docs`.
page_title: "{{ .Name }} {{ .Type }} - {{ .ProviderName }}"
subcategory: "Networking"
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{ .Name }} ({{ .Type }})

{{ .Description | trimspace }}

The list endpoint reports an id, a name and a description, and nothing else. Rules would be one more
call per group, so read a group with `dtcloud_security_group` when you need them.

## Example Usage

{{ tffile .ExampleFile }}

{{ .SchemaMarkdown | trimspace }}

## Behaviour worth knowing

* **`name` is applied by the provider**, since the list route takes no query parameters, and it can
  still match several groups.
* **`ids` follows the order of `security_groups`.** For one specific group, look it up with the
  `dtcloud_security_group` data source rather than indexing into the list.
