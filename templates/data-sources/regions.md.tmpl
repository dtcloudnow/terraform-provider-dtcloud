{{- /*
Page template for the dtcloud_regions data source: the one place for this page's
hand-written text. The summary sentence, the schema and the example are
generated; see the "Documentation" section of README.md. Section order: summary,
Example Usage, notes, Schema, Behaviour worth knowing.
*/ -}}
---
# generated by tfplugindocs -- edit templates/data-sources/regions.md.tmpl, the
# Description strings in dtcloud/region/datasource_regions.go, or the example under
# examples/data-sources/dtcloud_regions/, then run `make docs`.
page_title: "{{ .Name }} {{ .Type }} - {{ .ProviderName }}"
subcategory: "Account"
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{ .Name }} ({{ .Type }})

{{ .Description | trimspace }}

The region a resource is created in comes from the provider's `region_id`, not from here; this is
how to discover the valid values.

## Example Usage

{{ tffile .ExampleFile }}

{{ .SchemaMarkdown | trimspace }}

## Behaviour worth knowing

* **`available_services` lists only what is available**, sorted, so `contains()` is the natural check.
  A region the platform reports no availability information for has an empty list.
* **Changing the active region is not exposed.** The API endpoint for it switches the caller's
  session rather than any infrastructure; the provider takes its region through `region_id` instead.
