{{- /*
Page template for the dtcloud_volume_snapshots data source: the one place for
this page's hand-written text. The summary sentence, the schema and the example
are generated; see the "Documentation" section of README.md. Section order:
summary, Example Usage, notes, Schema, Behaviour worth knowing.
*/ -}}
---
# generated by tfplugindocs -- edit templates/data-sources/volume_snapshots.md.tmpl,
# the Description strings in dtcloud/volume/datasource_volume_snapshots.go, or the
# example under examples/data-sources/dtcloud_volume_snapshots/, then run
# `make docs`.
page_title: "{{ .Name }} {{ .Type }} - {{ .ProviderName }}"
subcategory: "Storage"
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{ .Name }} ({{ .Type }})

{{ .Description | trimspace }}

Read-only on purpose: snapshots are managed by `dtcloud_snapshot`, and creating one here would put two
resources in charge of the same object. What this is for is seeing what a destroy would take with
it, since deleting a volume deletes its snapshots too.

## Example Usage

{{ tffile .ExampleFile }}

{{ .SchemaMarkdown | trimspace }}

## Behaviour worth knowing

* **Newest first**, as the API sorts them by creation date.
* **A volume with no snapshots returns an empty list**; a volume that does not exist is an error.
