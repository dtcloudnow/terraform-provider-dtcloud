{{- /*
Page template for the dtcloud_vm data source: the one place for this page's
hand-written text. The summary sentence, the schema and the example are
generated; see the "Documentation" section of README.md. Section order: summary,
Example Usage, notes, Schema, Behaviour worth knowing.
*/ -}}
---
# generated by tfplugindocs -- edit templates/data-sources/vm.md.tmpl, the
# Description strings in dtcloud/vm/datasource_vm.go, or the example under
# examples/data-sources/dtcloud_vm/, then run `make docs`.
page_title: "{{ .Name }} {{ .Type }} - {{ .ProviderName }}"
subcategory: "Compute"
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{ .Name }} ({{ .Type }})

{{ .Description | trimspace }}

Lookup is by id because machine names are not unique: the platform holds several machines with the
same name, and its multi-create path generates `name-1`, `name-2` and so on, so a name could not
resolve to one instance.

## Example Usage

{{ tffile .ExampleFile }}

{{ .SchemaMarkdown | trimspace }}

## Behaviour worth knowing

* **Interfaces and volumes come from their own endpoints**, as on the resource. If one of them cannot
  be read, the lookup still succeeds with a warning and that list stays empty.
* **A machine that does not exist is an error**, not an empty result.
