{{- /*
Page template for the dtcloud_projects data source: the one place for this
page's hand-written text. The summary sentence, the schema and the example are
generated; see the "Documentation" section of README.md. Section order: summary,
Example Usage, notes, Schema, Behaviour worth knowing.
*/ -}}
---
# generated by tfplugindocs -- edit templates/data-sources/projects.md.tmpl, the
# Description strings in dtcloud/project/datasource_projects.go, or the example
# under examples/data-sources/dtcloud_projects/, then run `make docs`.
page_title: "{{ .Name }} {{ .Type }} - {{ .ProviderName }}"
subcategory: "Account"
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{ .Name }} ({{ .Type }})

{{ .Description | trimspace }}

## Example Usage

{{ tffile .ExampleFile }}

{{ .SchemaMarkdown | trimspace }}

## Behaviour worth knowing

~> **`active_project_id` is where everything lands.** Every resource this provider creates goes into
that project. Outputting it in a configuration that manages more than a handful of resources makes a
misconfigured credential obvious rather than surprising.

* **Changing the active project is not exposed.** The API has an endpoint for it, but it switches the
  caller's session rather than any infrastructure, so driving it from Terraform would silently move
  where every other call lands.
