---
# generated by tfplugindocs from the provider schema -- do not edit by hand.
# Edit the Description strings in the data source's Go file, the example under
# examples/data-sources/dtcloud_ssh_keys/, or templates/data-sources.md.tmpl, then run `make docs`.
page_title: "dtcloud_ssh_keys Data Source - dtcloud"
subcategory: "Access"
description: |-
  Lists the SSH keys on the account.
  The list endpoint reports less than the details one: a name and a creation timestamp, nothing else. Fingerprints and public keys would mean one extra call per key, so look a key up by name with dtcloud_ssh_key when you need those.
---

# dtcloud_ssh_keys (Data Source)

Lists the SSH keys on the account.

The list endpoint reports less than the details one: a name and a creation timestamp, nothing else. Fingerprints and public keys would mean one extra call per key, so look a key up by name with `dtcloud_ssh_key` when you need those.

## Example Usage

```terraform
data "dtcloud_ssh_keys" "all" {}

output "key_names" {
  value = data.dtcloud_ssh_keys.all.names
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `id` (String) The ID of this resource.
- `names` (List of String) Just the names, for the common case of checking whether a key exists.
- `ssh_keys` (List of Object) The SSH keys visible to the caller. (see [below for nested schema](#nestedatt--ssh_keys))

<a id="nestedatt--ssh_keys"></a>
### Nested Schema for `ssh_keys`

Read-Only:

- `created` (String)
- `name` (String)
