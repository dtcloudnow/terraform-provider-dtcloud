---
# generated by tfplugindocs from the provider schema -- do not edit by hand.
# Edit the Description strings in the data source's Go file, the example under
# examples/data-sources/dtcloud_vm_history/, or templates/data-sources.md.tmpl, then run `make docs`.
page_title: "dtcloud_vm_history Data Source - dtcloud"
subcategory: "Compute"
description: |-
  Lists what has been done to a virtual machine.
  This is an event log rather than desired state, which is why it is a data source and could never be a resource: there is nothing to create, change or destroy.
  It is worth a warning. The content changes every time anything happens to the machine -- including changes Terraform itself makes -- so feeding it into a resource argument produces a value that differs on every plan and a resource that never settles. Use it for outputs, for terraform console, or for a check outside the dependency graph.
---

# dtcloud_vm_history (Data Source)

Lists what has been done to a virtual machine.

This is an event log rather than desired state, which is why it is a data source and could never be a resource: there is nothing to create, change or destroy.

It is worth a warning. The content changes every time anything happens to the machine -- including changes Terraform itself makes -- so feeding it into a resource argument produces a value that differs on every plan and a resource that never settles. Use it for outputs, for `terraform console`, or for a check outside the dependency graph.

## Example Usage

```terraform
data "dtcloud_vm_history" "web" {
  vm_id = dtcloud_vm.web.id
}

output "last_action" {
  value = data.dtcloud_vm_history.web.entries[0].action
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `vm_id` (String) ID of the virtual machine.

### Read-Only

- `entries` (List of Object) Actions recorded against the VM, newest first as the API reports them. (see [below for nested schema](#nestedatt--entries))
- `id` (String) The ID of this resource.

<a id="nestedatt--entries"></a>
### Nested Schema for `entries`

Read-Only:

- `activity` (String)
- `date_and_time` (String)
- `id` (String)
- `initiator` (String)
