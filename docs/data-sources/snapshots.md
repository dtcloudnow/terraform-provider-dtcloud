---
# generated by tfplugindocs from the provider schema -- do not edit by hand.
# Edit the Description strings in the data source's Go file, the example under
# examples/data-sources/dtcloud_snapshots/, or templates/data-sources.md.tmpl, then run `make docs`.
page_title: "dtcloud_snapshots Data Source - dtcloud"
subcategory: "Storage"
description: |-
  Lists every snapshot visible to the caller.
  Not a duplicate of dtcloud_volume_snapshots, which reads a different endpoint scoped to one volume and sorted newest-first. This one is unfiltered and in the platform's own order, so the filters below are applied by the provider.
  The storage policy is reported both as the raw volume type id and as the resolved name, so this data source agrees with the rest of the provider.
---

# dtcloud_snapshots (Data Source)

Lists every snapshot visible to the caller.

Not a duplicate of `dtcloud_volume_snapshots`, which reads a different endpoint scoped to one volume and sorted newest-first. This one is unfiltered and in the platform's own order, so the filters below are applied by the provider.

The storage policy is reported both as the raw volume type id and as the resolved name, so this data source agrees with the rest of the provider.

## Example Usage

```terraform
data "dtcloud_snapshots" "ready" {
  status = "available"
}

data "dtcloud_snapshots" "of_data_volume" {
  volume_id = dtcloud_volume.data.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `name` (String) Only return snapshots with this exact name. Applied by the provider.
- `status` (String) Only return snapshots in this status, e.g. `available`. Applied by the provider.
- `volume_id` (String) Only return snapshots of this volume. Applied by the provider.

### Read-Only

- `id` (String) The ID of this resource.
- `snapshots` (List of Object) The snapshots that matched, in the order the platform returned them. (see [below for nested schema](#nestedatt--snapshots))

<a id="nestedatt--snapshots"></a>
### Nested Schema for `snapshots`

Read-Only:

- `created_at` (String)
- `description` (String)
- `id` (String)
- `name` (String)
- `size` (Number)
- `status` (String)
- `storage_policy` (String)
- `updated_at` (String)
- `volume_id` (String)
- `volume_type_id` (String)
