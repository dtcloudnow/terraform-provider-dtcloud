---
# generated by tfplugindocs from the provider schema -- do not edit by hand.
# Edit the Description strings in the data source's Go file, the example under
# examples/data-sources/dtcloud_ssh_key/, or templates/data-sources.md.tmpl, then run `make docs`.
page_title: "dtcloud_ssh_key Data Source - dtcloud"
subcategory: "Access"
description: |-
  Looks up a single SSH key by name.
  Use it to reference a key that Terraform did not create -- declaring one as a resource instead would hand Terraform ownership of it, and a later destroy would remove it.
---

# dtcloud_ssh_key (Data Source)

Looks up a single SSH key by name.

Use it to reference a key that Terraform did not create -- declaring one as a resource instead would hand Terraform ownership of it, and a later destroy would remove it.

## Example Usage

```terraform
data "dtcloud_ssh_key" "deploy" {
  name = "deploy-key"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) The name of the SSH key.

### Read-Only

- `created_at` (String) When the SSH key was created, in RFC 3339 format.
- `fingerprint` (String) The fingerprint of the SSH key.
- `id` (String) The ID of this resource.
- `public_key` (String) The public key material.
- `user_id` (String) The ID of the user that owns the SSH key.
