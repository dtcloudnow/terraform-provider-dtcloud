---
# generated by tfplugindocs from the provider schema -- do not edit by hand.
# Edit the Description strings in the data source's Go file, the example under
# examples/data-sources/dtcloud_elastic_ips/, or templates/data-sources.md.tmpl, then run `make docs`.
page_title: "dtcloud_elastic_ips Data Source - dtcloud"
subcategory: "Networking"
description: |-
  Lists the elastic IPs allocated to the caller.
  The most useful thing it answers is what you are paying for that nothing is using: an allocated address that is not associated still occupies quota and is still billed. status = "DOWN" is that question.
  Filters are applied by the provider, since the list route takes no query parameters. Every address is fetched either way and the API builds each row from three further calls of its own, so this is not a cheap read -- prefer dtcloud_elastic_ip when you already know which address you want.
---

# dtcloud_elastic_ips (Data Source)

Lists the elastic IPs allocated to the caller.

The most useful thing it answers is what you are paying for that nothing is using: an allocated address that is not associated still occupies quota and is still billed. `status = "DOWN"` is that question.

Filters are applied by the provider, since the list route takes no query parameters. Every address is fetched either way and the API builds each row from three further calls of its own, so this is not a cheap read -- prefer `dtcloud_elastic_ip` when you already know which address you want.

## Example Usage

```terraform
data "dtcloud_elastic_ips" "all" {}

# Allocated but attached to nothing: still billed, still using quota.
data "dtcloud_elastic_ips" "idle" {
  status = "DOWN"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `assigned_type` (String) Only return addresses attached to a `VM`, to an `LB`, or attached to nothing (`none`).
- `floating_network_id` (String) Only return addresses allocated from this external network.
- `status` (String) Only return addresses in this status: `ACTIVE`, `DOWN` or `ERROR`.

### Read-Only

- `elastic_ips` (List of Object) The addresses that matched. (see [below for nested schema](#nestedatt--elastic_ips))
- `id` (String) The ID of this resource.
- `ids` (List of String) Just the ids of the addresses that matched.
- `ip_addresses` (List of String) Just the addresses, in the same order as `ids`.

<a id="nestedatt--elastic_ips"></a>
### Nested Schema for `elastic_ips`

Read-Only:

- `assigned_id` (String)
- `assigned_to` (String)
- `assigned_type` (String)
- `fixed_ip_address` (String)
- `floating_network_id` (String)
- `id` (String)
- `ip_address` (String)
- `network_name` (String)
- `status` (String)
