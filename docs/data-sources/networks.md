---
# generated by tfplugindocs from the provider schema -- do not edit by hand.
# Edit the Description strings in the data source's Go file, the example under
# examples/data-sources/dtcloud_networks/, or templates/data-sources.md.tmpl, then run `make docs`.
page_title: "dtcloud_networks Data Source - dtcloud"
subcategory: "Networking"
description: |-
  Lists the networks visible to the caller.
  The list endpoint reports a flatter shape than the details one: it folds the subnet's CIDR and gateway up onto the network and reports DHCP as a word rather than a boolean. That is what is exposed here -- reading the full subnet for every network would mean one extra call each.
---

# dtcloud_networks (Data Source)

Lists the networks visible to the caller.

The list endpoint reports a flatter shape than the details one: it folds the subnet's CIDR and gateway up onto the network and reports DHCP as a word rather than a boolean. That is what is exposed here -- reading the full subnet for every network would mean one extra call each.

## Example Usage

```terraform
data "dtcloud_networks" "virtual" {
  network_type = "Virtual"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `name` (String) Only return networks whose name matches.
- `network_type` (String) Only return networks of this type: Virtual or Physical.

### Read-Only

- `id` (String) The ID of this resource.
- `networks` (List of Object) The networks that matched. (see [below for nested schema](#nestedatt--networks))

<a id="nestedatt--networks"></a>
### Nested Schema for `networks`

Read-Only:

- `cidr` (String)
- `dhcp` (String)
- `gateway` (String)
- `id` (String)
- `ipam` (String)
- `name` (String)
- `network_type` (String)
- `port_security_enabled` (Boolean)
- `subnet_id` (String)
