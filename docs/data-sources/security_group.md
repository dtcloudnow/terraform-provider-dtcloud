---
# generated by tfplugindocs from the provider schema -- do not edit by hand.
# Edit the Description strings in the data source's Go file, the example under
# examples/data-sources/dtcloud_security_group/, or templates/data-sources.md.tmpl, then run `make docs`.
page_title: "dtcloud_security_group Data Source - dtcloud"
subcategory: "Networking"
description: |-
  Looks up one security group, by id or by name.
  Name lookup exists because a security group id is the one value a machine cannot do without -- security_groups is required on every network interface -- and hand-copying a uuid out of the web panel is how that field gets filled otherwise.
  Names are not unique on the platform, so a name matching more than one group is an error rather than an arbitrary pick.
---

# dtcloud_security_group (Data Source)

Looks up one security group, by id or by name.

Name lookup exists because a security group id is the one value a machine cannot do without -- `security_groups` is required on every network interface -- and hand-copying a uuid out of the web panel is how that field gets filled otherwise.

Names are not unique on the platform, so a name matching more than one group is an error rather than an arbitrary pick.

## Example Usage

```terraform
# By id.
data "dtcloud_security_group" "web" {
  id = var.security_group_id
}

# Or by name -- the lookup fails rather than guessing if the name matches more
# than one group.
data "dtcloud_security_group" "app" {
  name = "app-tier"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `id` (String) ID of the security group to look up. Give this or `name`.
- `name` (String) Name of the security group to look up. Give this or `id`. Must match exactly one group.

### Read-Only

- `description` (String) Description of the security group.
- `inbound_rule` (List of Object) Inbound rules as the platform displays them. (see [below for nested schema](#nestedatt--inbound_rule))
- `outbound_rule` (List of Object) Outbound rules as the platform displays them. (see [below for nested schema](#nestedatt--outbound_rule))

<a id="nestedatt--inbound_rule"></a>
### Nested Schema for `inbound_rule`

Read-Only:

- `id` (String)
- `port_range` (String)
- `protocol` (String)
- `source` (String)


<a id="nestedatt--outbound_rule"></a>
### Nested Schema for `outbound_rule`

Read-Only:

- `id` (String)
- `port_range` (String)
- `protocol` (String)
- `source` (String)
