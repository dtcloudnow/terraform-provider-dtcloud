---
# generated by tfplugindocs from the provider schema -- do not edit by hand.
# Edit the Description strings on the resource's schema.Resource in dtcloud/, the
# example under examples/resources/dtcloud_ssh_key/, or templates/resources.md.tmpl,
# then run `make docs`.
page_title: "dtcloud_ssh_key Resource - dtcloud"
subcategory: "Access"
description: |-
  Manages an SSH key that can be injected into a virtual machine at boot.
  The API addresses keys by name and offers no update endpoint, so the Terraform ID is the key name and changing either name or public_key replaces the key.
---

# dtcloud_ssh_key (Resource)

Manages an SSH key that can be injected into a virtual machine at boot.

The API addresses keys by name and offers no update endpoint, so the Terraform ID is the key name and changing either `name` or `public_key` replaces the key.

## Example Usage

```terraform
resource "dtcloud_ssh_key" "deploy" {
  name       = "deploy-key"
  public_key = file("~/.ssh/id_ed25519.pub")
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) The name of the SSH key. Used as the resource identifier.
- `public_key` (String) The public key material.

### Optional

- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))

### Read-Only

- `created_at` (String) When the SSH key was created, in RFC 3339 format.
- `fingerprint` (String) The fingerprint of the SSH key.
- `id` (String) The ID of this resource.
- `user_id` (String) The ID of the user that owns the SSH key.

<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String)

## Import

Import is supported using the following syntax:

```shell
# SSH keys are addressed by name, not by id, so the name is the import id.
terraform import dtcloud_ssh_key.deploy deploy-key
```
