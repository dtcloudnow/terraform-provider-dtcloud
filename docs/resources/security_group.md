---
# generated by tfplugindocs from the provider schema -- do not edit by hand.
# Edit the Description strings on the resource's schema.Resource in dtcloud/, the
# example under examples/resources/dtcloud_security_group/, or templates/resources.md.tmpl,
# then run `make docs`.
page_title: "dtcloud_security_group Resource - dtcloud"
subcategory: "Networking"
description: |-
  Manages a security group -- the set of firewall rules applied to a machine's network interfaces.
  The group is only a name and a description. The rules inside it are separate dtcloud_security_group_rule resources, so adding or removing one rule leaves the group and every other rule untouched. Both arguments change in place; nothing here forces replacement, which matters because the platform refuses to delete a group that instances still reference.
  inbound_rule and outbound_rule are a read-only snapshot of what the platform displays, not something to configure.
  A description cannot be cleared once set: the SDK drops an empty description from the update request, so the old value would stay. The plan refuses this rather than proposing a change that never converges.
---

# dtcloud_security_group (Resource)

Manages a security group -- the set of firewall rules applied to a machine's network interfaces.

The group is only a name and a description. The rules inside it are separate `dtcloud_security_group_rule` resources, so adding or removing one rule leaves the group and every other rule untouched. Both arguments change in place; nothing here forces replacement, which matters because the platform refuses to delete a group that instances still reference.

`inbound_rule` and `outbound_rule` are a read-only snapshot of what the platform displays, not something to configure.

A description cannot be cleared once set: the SDK drops an empty description from the update request, so the old value would stay. The plan refuses this rather than proposing a change that never converges.

## Example Usage

```terraform
resource "dtcloud_security_group" "web" {
  name        = "web-tier"
  description = "HTTPS from anywhere, SSH from the office"
}

# Rules are separate resources, so adding or removing one leaves the group and
# every other rule untouched.
resource "dtcloud_security_group_rule" "https" {
  security_group_id = dtcloud_security_group.web.id
  direction         = "ingress"
  protocol          = "tcp"
  port_range_min    = 443
  port_range_max    = 443
  remote_ip_prefix  = "0.0.0.0/0"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the security group. Can be changed in place. Names are not required to be unique.

### Optional

- `description` (String) Description of the security group. Can be changed in place, but see the note on clearing it: an existing description cannot be emptied.
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))

### Read-Only

- `id` (String) The ID of this resource.
- `inbound_rule` (List of Object) Inbound rules as the platform displays them. Read-only; manage rules with dtcloud_security_group_rule. (see [below for nested schema](#nestedatt--inbound_rule))
- `outbound_rule` (List of Object) Outbound rules as the platform displays them. A new group starts with two, added by the platform: allow-all IPv4 and IPv6 egress. (see [below for nested schema](#nestedatt--outbound_rule))

<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String)
- `delete` (String)
- `update` (String)


<a id="nestedatt--inbound_rule"></a>
### Nested Schema for `inbound_rule`

Read-Only:

- `id` (String)
- `port_range` (String)
- `protocol` (String)
- `source` (String)


<a id="nestedatt--outbound_rule"></a>
### Nested Schema for `outbound_rule`

Read-Only:

- `id` (String)
- `port_range` (String)
- `protocol` (String)
- `source` (String)

## Import

Import is supported using the following syntax:

```shell
terraform import dtcloud_security_group.web 7f9c1e40-1c3d-4b6a-9a2f-5f1c9d0e3b11
```
